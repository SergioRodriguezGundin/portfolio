---
export interface ProjectCardProps {
  title: string;
  description: string;
  image: string;
}

const { title, description, image } = Astro.props as ProjectCardProps;
---

<div class='project-card'>
  <img src={image} alt={title} />
  <h3>{title}</h3>
  <p>{description}</p>
</div>
