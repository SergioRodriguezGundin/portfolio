<div class='card'>
  <div class='card-content border border-neutral-800 rounded-lg'>
    <slot />
  </div>
</div>

<style>
  .card {
    container: card / size;
    position: relative;
    height: 100%;
  }

  .card-content {
    height: 100%;
  }

  .card-title {
    position: absolute;
    top: 0;
    left: 0;
  }

  @container card (width < 400px) {
    .card-title {
      font-size: var(--font-size-base);
    }
  }

  @container card (width > 1080px) {
    .card-content {
      clip-path: polygon(22% 0,100% 0,100% 100%,0 100%,0 45px,18% 45px)
    }
  }
</style>
