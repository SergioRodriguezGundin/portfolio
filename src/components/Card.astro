---
interface CardProps {
  title: string;
}

const { title = 'title' } = Astro.props as CardProps;
---

<div class='card'>
  <h2 class='card-title text-secondary pl-4 xs:pt-3 xs:pl-2'>{title}</h2>
  <div class='card-content border-1 rounded-lg py-9 px-6'>
    <slot />
  </div>
</div>

<style>
  .card {
    container: card / size;
    position: relative;
    height: 100%;
  }

  .card-content {
    border: 1px solid var(--color-1000);
    clip-path: polygon(38% 0, 100% 0, 100% 100%, 0 100%, 0 45px, 30% 45px);
    height: 100%;
  }

  .card-title {
    position: absolute;
    top: 0;
    left: 0;
  }

  @container card (width < 400px) {
    .card-title {
      font-size: var(--font-size-base);
    }
  }

  @container card (width > 1080px) {
    .card-content {
      clip-path: polygon(22% 0,100% 0,100% 100%,0 100%,0 45px,18% 45px)
    }
  }
</style>
