---
import NumberTicker from '@components/magicui/number-ticker';


---

<div class='experience-container flex flex-row flex-space-around gap-2 px-4 py-6'>
  <div class='experience flex flex-column flex-start'>
    <span experience-years>
      <span>+</span>0<NumberTicker value={8} client:load/>
    </span>
    <span experience-text>Years of experience</span>
  </div>
  <div class='experience flex flex-column flex-start'>
    <span experience-years>
      <span>+</span>0<NumberTicker value={9} client:load/>
    </span>
    <span experience-text>Projects worked</span>
    </div>
  </div>
</div>

<style>
  .experience-container {
    container-type: inline-size;
    width: 100%;
    /*@container (max-width: 375px) {
      padding: @apply p-4;
    }*/
  }
  .experience {
    > span[experience-years] {
      font-size: var(--font-size-xxxl);
      font-weight: bold;
      > span {
        font-size: var(--font-size-lg);
        font-weight: 300;
      }
    }
    > span[experience-text] {
      font-size: var(--font-size-base);
    }
  }
  
  @media (768px <= width <= 1920px) {
    .experience {
      > span[experience-years] {
        font-size: var(--font-size-xl);
        > span {
          font-size: var(--font-size-base);
        }
      }
      > span[experience-text] {
        font-size: var(--font-size-sm);
      }
    }
  }
</style>
