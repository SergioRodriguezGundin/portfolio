<div class='absolute inline-block border-2 border-base-700 rounded-full m-auto transform-none cursor-pointer w-6 h-12 z-10'>
  <div class='mouse-wheel relative block bg-base-700 rounded-lg mx-2 my-1 w-1 h-3'></div>
</div>

<style>
  .mouse-wheel {
    animation: mouse-wheel 1.5s ease-in-out infinite;
    will-change: opacity, transform;
  }

  @keyframes mouse-wheel {
    0% {
      opacity: 1;
      transform: translateY(0);
    }
    100% {
      opacity: 0;
      transform: translateY(8px);
    }
  }
</style>
